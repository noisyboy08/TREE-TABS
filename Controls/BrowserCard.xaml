<UserControl x:Class="Sowser.Controls.BrowserCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:local="clr-namespace:Sowser.Controls"
             Width="700" Height="500" MinWidth="350" MinHeight="250"
             MouseEnter="Card_MouseEnter"
             MouseLeave="Card_MouseLeave">
    
    <Grid>
        <!-- Card Shadow with glow effect -->
        <Border Margin="12" CornerRadius="16">
            <Border.Background>
                <SolidColorBrush Color="#10FFFFFF"/>
            </Border.Background>
        </Border>
        
        <!-- Main Card - Glassmorphism -->
        <Border x:Name="CardBorder" Margin="12" CornerRadius="16" ClipToBounds="True"
                BorderBrush="#35FFFFFF" BorderThickness="1">
            <Border.Background>
                <!-- 60% Transparent Dark Glass (40% Opacity) -->
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#660A0F1A" Offset="0"/>
                    <GradientStop Color="#6605070D" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="4"/>   <!-- Group Color Strip -->
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="44"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- ── Feature #3: Group Color Accent Strip ── -->
                <Border x:Name="GroupColorStrip" Grid.Row="0" Background="Transparent"/>

                <!-- Tab Groups Row -->
                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" Margin="10,4,10,0" Height="28">
                    <StackPanel x:Name="TabsPanel" Orientation="Horizontal"/>
                </ScrollViewer>

                <!-- Title Bar -->
                <Border Grid.Row="2" Background="#15FFFFFF" BorderBrush="#25FFFFFF" BorderThickness="0,0,0,1"
                        Cursor="SizeAll"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                        MouseMove="TitleBar_MouseMove"
                        MouseLeftButtonUp="TitleBar_MouseLeftButtonUp">
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" Command="{x:Static local:BrowserCard.FullscreenCommand}"/>
                    </Border.InputBindings>
                    <Grid Margin="10,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Navigation Buttons -->
                        <Button x:Name="BackButton" Grid.Column="0" 
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="BackButton_Click" ToolTip="Back"
                                Width="30" Height="30" Padding="2" Foreground="#B0FFFFFF">
                            <materialDesign:PackIcon Kind="ChevronLeft" Width="18" Height="18"/>
                        </Button>
                        <Button x:Name="ForwardButton" Grid.Column="1"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="ForwardButton_Click" ToolTip="Forward"
                                Width="30" Height="30" Padding="2" Foreground="#B0FFFFFF">
                            <materialDesign:PackIcon Kind="ChevronRight" Width="18" Height="18"/>
                        </Button>
                        <Button x:Name="RefreshButton" Grid.Column="2"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="RefreshButton_Click" ToolTip="Refresh"
                                Width="30" Height="30" Padding="2" Margin="0,0,8,0" Foreground="#B0FFFFFF">
                            <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16"/>
                        </Button>
                        
                        <!-- URL Bar -->
                        <Border Grid.Column="3" Background="#20FFFFFF" CornerRadius="8" Margin="0,6"
                                BorderBrush="#30FFFFFF" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <materialDesign:PackIcon Kind="Web" Grid.Column="0" 
                                                         Width="14" Height="14"
                                                         VerticalAlignment="Center" Margin="10,0,6,0"
                                                         Foreground="#70FFFFFF"/>
                                <TextBlock x:Name="UrlTextBlock" Grid.Column="1" 
                                         VerticalAlignment="Center"
                                         Padding="0,6,12,6" FontSize="12"
                                         Foreground="#E0FFFFFF"
                                         TextTrimming="CharacterEllipsis"
                                         ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                            </Grid>
                        </Border>
                        
                        <!-- Pin Button -->
                        <Button x:Name="PinButton" Grid.Column="4"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="PinButton_Click" ToolTip="Pin to Viewport"
                                Width="30" Height="30" Padding="2" Margin="0,0,0,0" Foreground="#A9A9A9">
                            <materialDesign:PackIcon x:Name="PinIcon" Kind="PinOutline" Width="16" Height="16"/>
                        </Button>
                        
                        <!-- Ghost Button -->
                        <Button x:Name="GhostButton" Grid.Column="5"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="GhostButton_Click" ToolTip="Ghost Mode (Transparency)"
                                Width="30" Height="30" Padding="2" Margin="0,0,0,0" Foreground="#A9A9A9">
                            <materialDesign:PackIcon Kind="GhostOutline" Width="16" Height="16"/>
                        </Button>

                        <!-- Bookmark Button -->
                        <Button x:Name="BookmarkButton" Grid.Column="6"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="BookmarkButton_Click" ToolTip="Bookmark"
                                Width="30" Height="30" Padding="2" Margin="8,0,0,0" Foreground="#FFA502">
                            <materialDesign:PackIcon x:Name="BookmarkIcon" Kind="StarOutline" Width="16" Height="16"/>
                        </Button>
                        
                        <!-- Menu Button (3-line hamburger) -->
                        <Button x:Name="MenuButton" Grid.Column="7"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="MenuButton_Click" ToolTip="Menu"
                                Width="30" Height="30" Padding="2" Margin="4,0,0,0" Foreground="#A9A9A9">
                            <materialDesign:PackIcon Kind="Menu" Width="18" Height="18"/>
                        </Button>
                        
                        <!-- Close Button -->
                        <Button x:Name="CloseButton" Grid.Column="8"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="CloseButton_Click" ToolTip="Close"
                                Width="30" Height="30" Padding="2" Foreground="#FF6B6B">
                            <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                        </Button>
                    </Grid>
                </Border>
                
                <!-- WebView2 Content -->
                <wv2:WebView2 x:Name="WebView" Grid.Row="3"/>
            </Grid>
        </Border>

        <!-- Browser Menu Popup (3-line hamburger menu) -->
            <Popup x:Name="BrowserMenuPopup"
                   PlacementTarget="{Binding ElementName=MenuButton}"
                   Placement="Bottom"
                   StaysOpen="False"
                   AllowsTransparency="True"
                   PopupAnimation="Fade"
                   Panel.ZIndex="200">
                <Border x:Name="MenuPopupBorder"
                        Background="#1A1A2E"
                        BorderBrush="#35FFFFFF" BorderThickness="1"
                        CornerRadius="12"
                        Width="220">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.6"/>
                    </Border.Effect>
                    <StackPanel Margin="6">

                        <!-- Group 1: New Tab / Window -->
                        <Button x:Name="MenuNewTabBtn" Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuNewTab_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="PlusBoxOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="New Tab" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuNewWindow_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="OpenInNew" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="New Window" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>

                        <!-- Separator -->
                        <Separator Margin="6,4" Background="#30FFFFFF"/>

                        <!-- Group 2: Browser Features -->
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuHistory_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="History" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="History" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="BookmarkButton_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="StarOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Add to Bookmarks" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="MenuPinToggleBtn" Style="{StaticResource MaterialDesignFlatButton}"
                                Click="PinButton_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="MenuPinIcon" Kind="PinOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock x:Name="MenuPinText" Text="Pin to Viewport" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuDownloads_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="DownloadOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Downloads" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>

                        <!-- Group 4: Tab Groups Assignment -->
                        <Separator Margin="6,4" Background="#30FFFFFF"/>
                        <TextBlock Text="Assign to Group" Foreground="#80FFFFFF" FontSize="11" Margin="10,4,0,4"/>
                        <StackPanel x:Name="MenuGroupsList" Margin="4,0,4,4">
                            <!-- Populated dynamically in code-behind -->
                        </StackPanel>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                 Click="MenuClearGroup_Click" HorizontalContentAlignment="Left"
                                 Height="32" Padding="10,0" Foreground="#FF6B6B" Margin="0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FilterRemoveOutline" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Clear Group" VerticalAlignment="Center" FontSize="12"/>
                            </StackPanel>
                        </Button>
                        <Border Background="#15FFFFFF" CornerRadius="8" Margin="4,2" Padding="8,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Zoom" Foreground="#E0FFFFFF" FontSize="13" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Style="{StaticResource MaterialDesignIconButton}"
                                        Click="MenuZoomOut_Click" Width="28" Height="28" Foreground="#E0FFFFFF" ToolTip="Zoom Out">
                                    <materialDesign:PackIcon Kind="Minus" Width="14" Height="14"/>
                                </Button>
                                <TextBlock x:Name="MenuZoomLabel" Grid.Column="2" Text="100%" Foreground="#E0FFFFFF" FontSize="12"
                                           VerticalAlignment="Center" Margin="4,0" Width="36" TextAlignment="Center"/>
                                <Button Grid.Column="3" Style="{StaticResource MaterialDesignIconButton}"
                                        Click="MenuZoomIn_Click" Width="28" Height="28" Foreground="#E0FFFFFF" ToolTip="Zoom In">
                                    <materialDesign:PackIcon Kind="Plus" Width="14" Height="14"/>
                                </Button>
                                <Button Grid.Column="4" Style="{StaticResource MaterialDesignIconButton}"
                                        Click="MenuZoomFull_Click" Width="28" Height="28" Foreground="#E0FFFFFF" ToolTip="Fullscreen">
                                    <materialDesign:PackIcon Kind="Fullscreen" Width="14" Height="14"/>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- Separator -->
                        <Separator Margin="6,4" Background="#30FFFFFF"/>

                        <!-- Group 4: Tools -->
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuReadLater_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ClockOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Read later" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuCaptureToCanvas_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CameraOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Screenshot to canvas" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuPrint_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="PrinterOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Print..." VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuFindOnPage_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Magnify" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Find on Page" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuSavePage_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentSaveOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Save Page As..." VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>

                        <!-- Separator -->
                        <Separator Margin="6,4" Background="#30FFFFFF"/>

                        <!-- Group 5: Settings / Exit -->
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="MenuSettings_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#E0FFFFFF" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CogOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Settings" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Click="CloseButton_Click" HorizontalContentAlignment="Left"
                                Height="36" Padding="10,0" Foreground="#FF6B6B" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Close" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Close Tab" VerticalAlignment="Center" FontSize="13"/>
                            </StackPanel>
                        </Button>

                        <Border Height="4"/>
                    </StackPanel>
                </Border>
            </Popup>

        <!-- Connection Points Overlay -->
        <Grid x:Name="ConnectionOverlay" Margin="12" Visibility="Collapsed" Panel.ZIndex="20">
            <Grid.Resources>
                <Style x:Key="ConnectionPointStyle" TargetType="Border">
                    <Setter Property="Width" Value="14"/>
                    <Setter Property="Height" Value="14"/>
                    <Setter Property="CornerRadius" Value="7"/>
                    <Setter Property="Background" Value="#50FFFFFF"/>
                    <Setter Property="BorderBrush" Value="#70FFFFFF"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Opacity" Value="0.9"/>
                    <Setter Property="Cursor" Value="Cross"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#00D9FF"/>
                            <Setter Property="BorderBrush" Value="#00D9FF"/>
                            <Setter Property="Opacity" Value="1"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Grid.Resources>

            <Border Style="{StaticResource ConnectionPointStyle}" Tag="top"
                    VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,-6,0,0"
                    MouseLeftButtonDown="ConnectionPoint_MouseLeftButtonDown"
                    MouseEnter="ConnectionPoint_MouseEnter"
                    MouseLeave="ConnectionPoint_MouseLeave"/>

            <Border Style="{StaticResource ConnectionPointStyle}" Tag="right"
                    VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,-6,0"
                    MouseLeftButtonDown="ConnectionPoint_MouseLeftButtonDown"
                    MouseEnter="ConnectionPoint_MouseEnter"
                    MouseLeave="ConnectionPoint_MouseLeave"/>

            <Border Style="{StaticResource ConnectionPointStyle}" Tag="bottom"
                    VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,-6"
                    MouseLeftButtonDown="ConnectionPoint_MouseLeftButtonDown"
                    MouseEnter="ConnectionPoint_MouseEnter"
                    MouseLeave="ConnectionPoint_MouseLeave"/>

            <Border Style="{StaticResource ConnectionPointStyle}" Tag="left"
                    VerticalAlignment="Center" HorizontalAlignment="Left" Margin="-6,0,0,0"
                    MouseLeftButtonDown="ConnectionPoint_MouseLeftButtonDown"
                    MouseEnter="ConnectionPoint_MouseEnter"
                    MouseLeave="ConnectionPoint_MouseLeave"/>
        </Grid>
        
        <!-- Resize Handles (outside card for better mouse capture) -->
        <Rectangle x:Name="ResizeHandleW" Width="12" HorizontalAlignment="Left" VerticalAlignment="Stretch"
                   Fill="Transparent" Cursor="SizeWE" Margin="0,20,0,20"
                   MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                   MouseMove="ResizeHandle_MouseMove" MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp"/>
        <Rectangle x:Name="ResizeHandleE" Width="12" HorizontalAlignment="Right" VerticalAlignment="Stretch"
                   Fill="Transparent" Cursor="SizeWE" Margin="0,20,0,20"
                   MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                   MouseMove="ResizeHandle_MouseMove" MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp"/>
        <Rectangle x:Name="ResizeHandleN" Height="12" HorizontalAlignment="Stretch" VerticalAlignment="Top"
                   Fill="Transparent" Cursor="SizeNS" Margin="20,0,20,0"
                   MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                   MouseMove="ResizeHandle_MouseMove" MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp"/>
        <Rectangle x:Name="ResizeHandleS" Height="12" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
                   Fill="Transparent" Cursor="SizeNS" Margin="20,0,20,0"
                   MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                   MouseMove="ResizeHandle_MouseMove" MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp"/>
        
        <!-- Corner Handles -->
        <Rectangle x:Name="ResizeHandleNW" Width="16" Height="16" HorizontalAlignment="Left" VerticalAlignment="Top"
                   Fill="Transparent" Cursor="SizeNWSE"
                   MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                   MouseMove="ResizeHandle_MouseMove" MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp"/>
        <Rectangle x:Name="ResizeHandleNE" Width="16" Height="16" HorizontalAlignment="Right" VerticalAlignment="Top"
                   Fill="Transparent" Cursor="SizeNESW"
                   MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                   MouseMove="ResizeHandle_MouseMove" MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp"/>
        <Rectangle x:Name="ResizeHandleSW" Width="16" Height="16" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                   Fill="Transparent" Cursor="SizeNESW"
                   MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                   MouseMove="ResizeHandle_MouseMove" MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp"/>
        <Rectangle x:Name="ResizeHandleSE" Width="16" Height="16" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Fill="Transparent" Cursor="SizeNWSE"
                   MouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown"
                   MouseMove="ResizeHandle_MouseMove" MouseLeftButtonUp="ResizeHandle_MouseLeftButtonUp"/>
    </Grid>
</UserControl>
